mod page;
mod database;
mod bytebuffer;
mod values;
mod varint;
mod record;



#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
